import AuthForm from "../features/auth/AuthForm";

const Auth = () => {
  return (
    <div className="flex min-h-[100vh] items-center justify-center px-2">
      <AuthForm />
    </div>
  );
};

export default Auth;
