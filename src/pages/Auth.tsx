import AuthForm from "../components/AuthForm";

const Auth = () => {
  return (
    <div className="flex min-h-[100vh] items-center justify-center">
      <AuthForm />
    </div>
  );
};

export default Auth;
