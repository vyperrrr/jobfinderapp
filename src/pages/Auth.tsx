import { Section } from "@radix-ui/themes";
import AuthForm from "../components/AuthForm";

const Auth = () => {
  return (
    <Section>
      <AuthForm />
    </Section>
  );
};

export default Auth;
